namespace BayesianPG.ThreePG
{
    public enum ThreePGHeightModel
    {
        // specify values since Enum.Parse<>() supports integers as well as names
        Power = 1,
        Exponent = 2
    }
}
